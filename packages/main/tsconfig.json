{
  "extends": "../../tsconfig-settings.json",
  "compilerOptions": {
    "sourceMap": false,
    "skipLibCheck": true,
    // Don't automatically include anything.
    // Only include `@types` packages that we need to import.
    "types": [],
    "baseUrl": ".",
    "paths": {
      "bonjour-hap": [
        "../../types/bonjour-hap"
      ],
      "/@common/*": [
        "../common/*"
      ]
    }
  },
  "include": [
    "../common/*.ts",
    "src/**/*.ts",
    "../../types/**/*.d.ts"
, "../renderer/gmib/util/authBaseQuery.ts", "../renderer/authBaseQuery.ts"  ],
  "exclude": [
    "**/*.spec.ts",
    "**/*.test.ts",
    "../common/prepareDrawArgs.ts",
  ]
}
